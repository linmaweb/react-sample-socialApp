import React from "react";

const Title = ({ title }) => {
  return (
    <div className="header">
      <h1 className="title">React Sample Project: {title}</h1>
    </div>
  );
};

export default Title;
